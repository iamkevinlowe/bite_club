<nav class="grid nav-header">
  <div class="eighth logo">
    <%= link_to inline_svg('logo1'), :root %>
  </div>

  <div class="three-fourths content">
    <% if local_assigns[:resource] && resource.respond_to?(:name) %>
      <%= content_tag(:span, resource.name, class: 'list-name') %>
    <% end %>
  </div>

  <div class="eighth fb-share">
    <%= content_tag(:div, nil, class: 'fb-share-button', data: { href: request.original_url, layout: 'button' }) %>
  </div>
</nav>